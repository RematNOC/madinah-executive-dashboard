import './globals.css';

export const metadata = {
  title: 'Madinah AI Transformation Roadmap',
  description: 'Digital Systems Overhaul for Budget Jewelry Retail (30+ Stores)',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
